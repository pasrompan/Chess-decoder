# Environment Variables Template
# Copy this file to .env and fill in your actual values
# The .env file is ignored by git for security

# Google Cloud Configuration
# For local development, you can either:
# 1. Set up Application Default Credentials (recommended)
# 2. Use a service account key file
# 3. Leave these empty to run in local-only mode

# Option 1: Application Default Credentials (recommended)
# Run: gcloud auth application-default login
# Then leave these empty:
# GOOGLE_APPLICATION_CREDENTIALS=

# Option 2: Service Account Key File
# Download a service account key from Google Cloud Console
# and set the path to the JSON file:
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# Google Cloud Storage Bucket Names
# Replace with your actual bucket names
GOOGLE_CLOUD_DATABASE_BUCKET=chessdecoder-db-adept-amp-458515-j9
GOOGLE_CLOUD_IMAGES_BUCKET=chessdecoder-images-adept-amp-458515-j9

# Database Configuration
# For local development, this will use SQLite
DATABASE_CONNECTION_STRING=Data Source=data/chessdecoder.db

# JWT Configuration (if using authentication)
JWT_SECRET_KEY=your-super-secret-jwt-key-here
JWT_ISSUER=ChessDecoderApi
JWT_AUDIENCE=ChessDecoderApi

# Application Settings
ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE_URLS=http://localhost:5000;https://localhost:5001

# CORS Origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,https://chess-scribe-convert.lovable.app

# Test Authentication (for E2E testing only)
# Set to "true" to enable username/password login for local development and CI
# WARNING: Never enable in production!
ENABLE_TEST_AUTH=false
